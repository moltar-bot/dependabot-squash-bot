{
  "name": "dependabot-squash-bot",
  "version": "0.1.0",
  "description": "GitHub Action to auto-squash-merge Dependabot PRs when checks pass",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests configured'"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "semver": "^7.6.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
